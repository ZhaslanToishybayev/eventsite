# 🌐 Настройка домена fan-club.kz

## Важно: Что нужно сделать

Сайт сейчас работает на Django development server, но для полноценной работы по домену fan-club.kz нужно выполнить несколько шагов:

### 1. Настроить Nginx (выполните эти команды)

```bash
cd /var/www/myapp/eventsite

# Сделать скрипт исполняемым
chmod +x setup_nginx.sh

# Запустить настройку Nginx (потребуются права sudo)
sudo ./setup_nginx.sh
```

### 2. Проверить DNS настройки

Убедитесь, что DNS записи для fan-club.kz указывают на IP вашего сервера:
- **A запись**: fan-club.kz → 77.243.80.110
- **A запись**: www.fan-club.kz → 77.243.80.110

### 3. Запустить Django сервер

```bash
cd /var/www/myapp/eventsite
source venv/bin/activate
python manage.py runserver 127.0.0.1:8000
```

### 4. Установить SSL сертификаты (рекомендуется)

```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d fan-club.kz -d www.fan-club.kz
```

### 5. Настроить systemd сервис (для автозапуска)

```bash
# Скопировать сервис файл
sudo cp unitysphere.service /etc/systemd/system/

# Включить и запустить сервис
sudo systemctl daemon-reload
sudo systemctl enable unitysphere
sudo systemctl start unitysphere
```

## 🌐 Доступ к сайту после настройки

После выполнения этих шагов сайт будет доступен по:

- **HTTP**: http://fan-club.kz
- **HTTP**: http://www.fan-club.kz
- **HTTPS**: https://fan-club.kz (после SSL)
- **HTTPS**: https://www.fan-club.kz (после SSL)
- **По IP**: http://77.243.80.110

## 🔧 Текущий статус

Сейчас сайт работает в development режиме:
- **Django development server** на 127.0.0.1:8000
- **Nginx** как reverse proxy (нужно настроить)
- **SQLite** база данных
- **DEBUG=True** (только для разработки)

## 📝 Что уже готово

✅ **Django приложение** - полностью функционирует
✅ **Nginx конфигурация** - создана и готова
✅ **SSL настройка** - инструкции готовы
✅ **Systemd сервис** - файл создан
✅ **OpenAI интеграция** - настроена
✅ **AI агент** - работает
✅ **Шаблоны** - готовы
✅ **URL маршруты** - настроены

## 🚀 Быстрый запуск по домену

Выполните эти команды для быстрой настройки:

```bash
# 1. Настроить Nginx
cd /var/www/myapp/eventsite
chmod +x setup_nginx.sh
sudo ./setup_nginx.sh

# 2. Запустить Django сервер
source venv/bin/activate
python manage.py runserver 127.0.0.1:8000

# 3. Проверить сайт
curl -I http://fan-club.kz
```

После этого сайт будет доступен по fan-club.kz! 🎉