# 🚀 UnitySphere Production Deployment Guide

## 📋 Идеальный путь для запуска сайта на fan-club.kz

Этот гайд поможет вам полностью настроить production-сервер с работающим AI виджетом.

### 🔧 Подготовка

1. **Убедитесь что Django работает:**
   ```bash
   cd /var/www/myapp/eventsite
   source venv/bin/activate
   python manage.py runserver 0.0.0.0:8080 --insecure
   ```

2. **Проверьте доступность:**
   ```bash
   curl http://127.0.0.1:8080/
   ```

### 📦 Запуск финальной настройки

Выполните один из этих скриптов:

#### Вариант 1: Полный автоматический запуск
```bash
chmod +x /var/www/myapp/eventsite/final_production_launch.sh
./final_production_launch.sh
```

#### Вариант 2: Только nginx настройка
```bash
chmod +x /var/www/myapp/eventsite/setup_production_nginx.sh
./setup_production_nginx.sh
```

### 🔧 Ручная настройка (если нет sudo доступа)

Если скрипты не работают из-за отсутствия sudo доступа:

1. **Скопируйте nginx конфигурацию:**
   ```bash
   sudo cp /tmp/nginx_production.conf /etc/nginx/nginx.conf
   ```

2. **Проверьте конфигурацию:**
   ```bash
   sudo nginx -t
   ```

3. **Перезапустите nginx:**
   ```bash
   sudo systemctl restart nginx
   ```

### 🌐 Адреса для проверки

После настройки сайт должен быть доступен по:

- **http://fan-club.kz/**
- **http://www.fan-club.kz/**
- **http://77.243.80.110/**

### 🤖 AI Виджет

AI виджет должен работать по адресу:
```
/api/v1/ai/production/agent/
```

### 📊 Проверка работоспособности

#### Проверка Django сервера:
```bash
curl http://127.0.0.1:8080/
```

#### Проверка AI виджета:
```bash
curl http://127.0.0.1:8080/api/v1/ai/production/agent/
```

#### Проверка через nginx:
```bash
curl http://fan-club.kz/
curl http://fan-club.kz/api/v1/ai/production/agent/
```

### 🛠️ Возможные проблемы и решения

#### Проблема: Сайт недоступен
**Решение:**
1. Проверьте что Django запущен на порту 8080
2. Проверьте nginx конфигурацию
3. Перезапустите nginx

#### Проблема: AI виджет не работает
**Решение:**
1. Проверьте Django URL конфигурацию
2. Убедитесь что `/api/v1/ai/production/agent/` маршрут существует
3. Проверьте AI агент логи

#### Проблема: 404 ошибки
**Решение:**
1. Проверьте ALLOWED_HOSTS в settings.py
2. Убедитесь что nginx правильно проксирует на Django
3. Проверьте статические файлы

### 🔍 Диагностика

#### Проверка статуса сервисов:
```bash
# Django статус
curl -I http://127.0.0.1:8080/

# nginx статус
curl -I http://fan-club.kz/

# Проверка процессов
ps aux | grep python
ps aux | grep nginx
```

#### Проверка логов:
```bash
# Django логи
tail -f /var/log/django.log

# nginx логи
sudo tail -f /var/log/nginx/error.log
```

### 📁 Важные файлы

- **Nginx конфигурация:** `/etc/nginx/nginx.conf`
- **Django settings:** `/var/www/myapp/eventsite/core/settings.py`
- **URL конфигурация:** `/var/www/myapp/eventsite/core/urls.py`
- **AI агент:** `/var/www/myapp/eventsite/ai_consultant/agents/lightweight_production_agent.py`

### 🎯 Финальная проверка

После всех настроек выполните полную проверку:

```bash
echo "=== ПРОВЕРКА САЙТА ==="
curl -I http://fan-club.kz/
echo ""

echo "=== ПРОВЕРКА AI ВИДЖЕТА ==="
curl -I http://fan-club.kz/api/v1/ai/production/agent/
echo ""

echo "=== ПРОВЕРКА СТАТИЧЕСКИХ ФАЙЛОВ ==="
curl -I http://fan-club.kz/static/css/style.css
echo ""

echo "=== ПРОВЕРКА МЕДИА ФАЙЛОВ ==="
curl -I http://fan-club.kz/media/clubs/logo1.png
echo ""
```

### 🎉 Готово!

Если все проверки проходят успешно, ваш сайт UnitySphere с AI виджетом полностью настроен и готов к использованию!

---

## 📞 Поддержка

Если возникнут проблемы:

1. **Проверьте логи Django и nginx**
2. **Убедитесь что все процессы запущены**
3. **Проверьте конфигурацию сети и firewall**
4. **Проверьте права доступа к файлам**

**Контакты для помощи:**
- Django процессы: `ps aux | grep python`
- nginx процессы: `ps aux | grep nginx`
- Статус nginx: `sudo systemctl status nginx`