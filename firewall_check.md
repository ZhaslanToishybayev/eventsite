# 🔥 Проверка и настройка фаервола

## Проблема
Сайт работает локально (http://127.0.0.1 показывает UnitySphere), но не открывается по доменному имени.

## Решение - проверить фаервол

Выполните следующие команды с sudo:

### 1. Проверить статус фаервола
```bash
sudo ufw status
```

### 2. Если фаервол включен, разрешить HTTP и HTTPS
```bash
sudo ufw allow 80
sudo ufw allow 443
```

### 3. Или временно отключить фаервол для тестирования
```bash
sudo ufw disable
```

### 4. Проверить, что порт 80 теперь доступен извне
```bash
# Проверить прослушивание портов
sudo netstat -tulpn | grep :80
```

### 5. Проверить доступность сайта
Попробуйте открыть в браузере: http://fan-club.kz

## Альтернатива - проверить iptables
Если ufw не используется, проверьте iptables:
```bash
sudo iptables -L -n
```

## Проверка соединения
Проверьте, что сервер принимает внешние соединения:
```bash
# Проверить, что Nginx слушает на всех интерфейсах
ss -tulpn | grep :80
```

Должно показывать:
- 0.0.0.0:80 (слушает на всех интерфейсах)
- не только 127.0.0.1:80 (только локально)