# 🏗️ Руководство по рефакторингу ИИ-консультанта v2.0

## 📋 **Обзор рефакторинга**

Монолитный `AIConsultantService` (1265 строк) разбит на специализированные сервисы с четким разделением ответственности.

---

## 🎯 **Архитектура v2.0**

### 📁 **Новая структура файлов:**
```
ai_consultant/
├── services_v2.py              # Основной координатор сервисов
├── services/                   # Директория сервисов
│   ├── __init__.py
│   ├── chat.py                # Сервис чат-сессий
│   ├── context.py             # Сервис контекста ИИ
│   ├── openai_client.py       # OpenAI клиент
│   └── message_processor.py   # Обработка сообщений
├── api/
│   ├── views_v2.py            # API v2.0
│   └── urls_v2.py             # URL v2.0
└── services.py                # Старый сервис (для совместимости)
```

### 🔄 **Принципы архитектуры:**

1. **Single Responsibility** - каждый сервис выполняет одну функцию
2. **Dependency Injection** - внедрение зависимостей
3. **Caching** - кэширование на всех уровнях
4. **Error Handling** - централизованная обработка ошибок
5. **Health Checks** - мониторинг работоспособности

---

## 🚀 **Основные компоненты**

### 1️⃣ **AIConsultantServiceV2** (Координатор)
```python
class AIConsultantServiceV2:
    """
    Основной сервис-координатор
    """
    def __init__(self):
        self.openai_service = OpenAIClientService()
        self.chat_service = ChatService(self.openai_service)
        self.context_service = ContextService()
        self.message_processor = MessageProcessorService()
```

**Ответственности:**
- Координация работы сервисов
- Управление кэшированием
- Очистка старых данных
- Сбор аналитики

### 2️⃣ **ChatService** (Управление чатом)
```python
class ChatService(BaseAIService):
    """
    Управление сессиями и сообщениями
    """
```

**Ответственности:**
- Создание/удаление сессий
- Сохранение сообщений
- История чата
- Статистика пользователя

### 3️⃣ **OpenAIClientService** (OpenAI интеграция)
```python
class OpenAIClientService(BaseAIService):
    """
    Оптимизированная работа с OpenAI API
    """
```

**Ответственности:**
- Запросы к OpenAI API
- Обработка токенов
- Кэширование ответов
- Управление моделями

### 4️⃣ **ContextService** (Контекст ИИ)
```python
class ContextService(BaseAIService):
    """
    Управление системным контекстом
    """
```

**Ответственности:**
- Системные промпты
- Персонализация
- Управление контекстами
- Кэширование контекстов

### 5️⃣ **MessageProcessorService** (Обработка сообщений)
```python
class MessageProcessorService(BaseAIService):
    """
    Предварительная и постобработка сообщений
    """
```

**Ответственности:**
- Валидация и очистка
- Форматирование
- Определение намерений
- Извлечение ключевых слов

---

## 📡 **API Эндпоинты v2.0**

### Основные чат эндпоинты:
- `POST /api/v1/ai/chat/v2/` - отправить сообщение
- `GET /api/v1/ai/chat/v2/?session_id=UUID` - история чата
- `POST /api/v1/ai/sessions/v2/create/` - создать сессию
- `GET /api/v1/ai/sessions/v2/` - список сессий

### Управление сессиями:
- `DELETE /api/v1/ai/sessions/v2/<id>/delete/` - удалить сессию
- `POST /api/v1/ai/sessions/v2/<id>/title/` - обновить заголовок
- `POST /api/v1/ai/sessions/v2/<id>/archive/` - архивировать

### Аналитика и мониторинг:
- `GET /api/v1/ai/analytics/v2/` - аналитика пользователя
- `GET /api/v1/ai/health/v2/` - проверка здоровья
- `GET /api/v1/ai/status/v2/` - статус сервисов
- `DELETE /api/v1/ai/cache/v2/clear/` - очистить кэш

---

## ⚡ **Производительность и кэширование**

### 🎯 **Стратегии кэширования:**

1. **Ответы OpenAI**: 5 минут
   ```python
   cache_key = f"openai_response_{hash(messages)}"
   cache.set(cache_key, response, 300)
   ```

2. **История чата**: 5 минут
   ```python
   cache_key = f"chat_history_{session_id}_{limit}"
   cache.set(cache_key, history, 300)
   ```

3. **Системный контекст**: 1 час
   ```python
   cache_key = "system_context_primary"
   cache.set(cache_key, context, 3600)
   ```

4. **Аналитика пользователя**: 10 минут
   ```python
   cache_key = f"user_analytics_{user_id}"
   cache.set(cache_key, analytics, 600)
   ```

### 📊 **Оптимизации:**

- **Лимит истории**: максимум 50 сообщений
- **Очистка старых данных**: автоматическая после 100 сообщений
- **Обрезка токенов**: умное сокращение контекста
- **Асинхронная обработка**: подготовка в фоне

---

## 🔧 **Использование**

### 🚀 **Быстрый старт:**
```python
# Создание сервиса
ai_service = AIServiceFactory.create_chat_service()

# Создание сессии
session = ai_service.create_chat_session(user)

# Отправка сообщения
response = ai_service.send_message(session, "Привет!")

# Получение истории
history = ai_service.get_chat_history(session)
```

### 🎨 **Продвинутое использование:**
```python
# Создание отдельных сервисов
chat_service = AIServiceFactory.create_chat_service_only()
context_service = AIServiceFactory.create_context_service()

# Персонализированный контекст
personal_context = context_service.get_personalized_context(user)

# Обработка сообщений
processor = MessageProcessorService()
intent = processor.detect_intent(message)
```

---

## 🔄 **Миграция с v1.0**

### 📋 **Шаги миграции:**

1. **Обновление зависимостей:**
   ```python
   # Старый код
   from ai_consultant.services import AIConsultantService

   # Новый код
   from ai_consultant.services_v2 import AIConsultantServiceV2
   ```

2. **Изменение инициализации:**
   ```python
   # Старый код
   service = AIConsultantService()

   # Новый код
   service = AIServiceFactory.create_chat_service()
   ```

3. **Обновление API эндпоинтов:**
   ```javascript
   // Старый эндпоинт
   '/api/v1/ai/chat/'

   // Новый эндпоинт
   '/api/v1/ai/chat/v2/'
   ```

### ✅ **Обратная совместимость:**

- Старые эндпоинты продолжают работать
- Можно постепенно мигрировать на v2
- Автоматическое переключение при ошибках

---

## 🧪 **Тестирование**

### 🎯 **Unit тесты:**
```python
def test_openai_service():
    service = OpenAIClientService()
    assert service.is_available()

    response = service.simple_completion("Say 'test'")
    assert "test" in response.lower()

def test_message_processor():
    processor = MessageProcessorService()

    processed = processor.preprocess("  Hello!  ")
    assert processed == "Hello!"

    intent = processor.detect_intent("Как создать клуб?")
    assert "club_creation" in intent["intents"]
```

### 🔍 **Health Checks:**
```python
# Проверка всех сервисов
health = ai_service.health_check()
assert health["status"] == "healthy"

# Проверка отдельных сервисов
assert ai_service.openai_service.is_available()
assert ai_service.chat_service.health_check()
```

---

## 📈 **Мониторинг**

### 📊 **Метрики производительности:**
- Время ответа OpenAI API
- Количество запросов в минуту
- Hit rate кэша
- Размер истории чатов

### 🚨 **Алерты:**
- OpenAI API недоступен
- Время ответа > 5 секунд
- Rate limit превышен
- Ошибки БД

### 📋 **Логирование:**
```python
logger.info(f"Сообщение обработано", {
    'session_id': session.id,
    'tokens_used': tokens_used,
    'response_time': response_time
})
```

---

## 🔮 **Будущие улучшения**

### 🎯 **Планируемые функции:**

1. **🔄 Event-driven архитектура**
   - События для всех действий
   - Асинхронная обработка
   - Очереди сообщений

2. **🤖 ML модели**
   - Классификация намерений
   - Анализ тональности
   - Рекомендации

3. **📊 Расширенная аналитика**
   - Воронки диалогов
   - Тепловые карты
   - Предиктивная аналитика

4. **🌐 Multi-language поддержка**
   - Определение языка
   - Перевод контекста
   - Локализация

---

## 🎯 **Лучшие практики**

### ✅ **Рекомендации:**
1. **Используйте фабрики** для создания сервисов
2. **Кэшируйте** повторяющиеся запросы
3. **Логируйте** все бизнес-операции
4. **Обрабатывайте ошибки** на всех уровнях
5. **Проверяйте здоровье** сервисов регулярно

### ❌ **Избегайте:**
1. Прямых вызовов OpenAI без сервиса
2. Хранения больших данных в памяти
3. Игнорирования кэша
4. Монолитных функций
5. Отсутствия логов

---

## 📞 **Поддержка**

### 🔗 **Полезные ресурсы:**
- **API v2.0**: `/api/v1/ai/health/v2/`
- **Документация**: `AI_REFACTORING_GUIDE.md`
- **Демо**: `/ai-chat-demo-v2/`
- **Статус**: `/api/v1/ai/status/v2/`

### 🐛 **Отладка:**
```python
# Включение детального логирования
import logging
logging.getLogger('ai_consultant').setLevel(logging.DEBUG)

# Проверка сервисов
health = get_ai_service_health()
print(f"Статус: {health['status']}")
```

**🎉 Рефакторинг завершен! ИИ-консультант теперь имеет масштабируемую архитектуру!**