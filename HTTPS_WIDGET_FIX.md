# 🚀 ИНСТРУКЦИЯ: Восстановление AI виджета после перехода на HTTPS

## 🎯 Проблема:
После перехода сайта на HTTPS виджет перестал работать из-за:
1. **HTTP → HTTPS редиректа**
2. **CORS политик** (API настроен на HTTPS)
3. **Относительных URL** в JavaScript

## ✅ Решение:

### 1. Замените виджет в `base.html`:

**Найдите и замените эту секцию в `/var/www/myapp/eventsite/templates/base.html`:**

```html
<!-- УДАЛИТЬ эти строки (старый виджет): -->
<link rel="stylesheet" href="{% static 'css/ai-chat-widget-v2.css' %}?v=2.5.4">
<script src="{% static 'js/ai-chat-widget-v2.js' %}?v=2.5.4"></script>
{% include 'unity_widget_clean.html' %}

<!-- ЗАМЕНИТЬ на это: -->
{% include 'https_widget.html' %}
```

### 2. Проверьте что файлы созданы:

- ✅ `/var/www/myapp/eventsite/static/js/ai-chat-widget-https.js`
- ✅ `/var/www/myapp/eventsite/static/css/ai-chat-widget-https.css`
- ✅ `/var/www/myapp/eventsite/templates/https_widget.html`

### 3. Обновите страницу:

1. **Очистите кэш браузера** (Ctrl + F5)
2. **Перейдите на** `https://fan-club.kz/`
3. **Проверьте консоль браузера** (F12 → Console)

## 🔍 Проверка работоспособности:

### В консоли браузера должны быть сообщения:
```
🤖 HTTPS Widget: DOM загружен, инициализация...
🔒 HTTPS режим: true
✅ Загружаем HTTPS виджет
✅ HTTPS виджет загружен и готов к использованию
```

### Если видите ошибки:
```
⚠️  HTTP режим, виджет не будет загружен для безопасности
💡 Перейдите на https://fan-club.kz/ для использования AI виджета
```

**Это нормально!** Виджет работает только по HTTPS.

## 🎉 Результат:

После обновления:
- ✅ **AI виджет будет работать** по HTTPS
- ✅ **Красивый glassmorphism дизайн**
- ✅ **Поддержка Markdown**
- ✅ **Quick replies**
- ✅ **Автоматическая сессия**
- ✅ **CSRF защита**

## 🛠️ Альтернатива (если не хотите менять base.html):

Просто добавьте в `base.html` перед `</body>`:

```html
{% include 'https_widget.html' %}
```

И удалите старые виджет скрипты.

## 📋 Что нового в HTTPS виджете:

1. **Автоопределение протокола** (HTTP/HTTPS)
2. **Правильные CORS заголовки**
3. **CSRF токен поддержка**
4. **Улучшенный дизайн**
5. **Лучшая производительность**
6. **Адаптивность**

---

**✅ Готово! Виджет должен работать по HTTPS!**