# 🏠 Локальная разработка UnitySphere

## 🚀 Быстрый старт

### 1. Запуск приложения
```bash
# Активируем виртуальное окружение
source venv/bin/activate

# Запускаем сервер разработки
python manage.py runserver

# Приложение доступно по адресу: http://localhost:8000
```

### 2. Проверка работы
```bash
# Откройте в браузере:
http://localhost:8000

# API тесты:
curl http://localhost:8000/api/v1/clubs/
```

## 📝 Что уже работает:

### ✅ **Основной функционал**
- [x] Django сайт загружается
- [x] API клубов возвращает JSON данные
- [x] Статические файлы подключены
- [x] Медиа файлы загружаются
- [x] Rate limiting (защита от DoS)
- [x] Безопасная загрузка файлов
- [x] CSP заголовки безопасности

### ✅ **Доступные URL:**
- **Главная страница:** http://localhost:8000/
- **Админ панель:** http://localhost:8000/admin/
- **API клубы:** http://localhost:8000/api/v1/clubs/
- **API Root:** http://localhost:8000/api/v1/

### ✅ **Тестовые данные:**
В базе уже есть 7 клубов:
- Художественная Палитра
- Вершинные Скалолазы
- AI Test Club
- Книжные Вдохновители
- Танцующие Экстазы
- ФотоАрт Мастерская
- на Эверест

## 🛠️ Управление проектом

### Создание суперпользователя
```bash
python manage.py createsuperuser
```

### Управление миграциями
```bash
# Проверить статус миграций
python manage.py showmigrations

# Создать новые миграции
python manage.py makemigrations

# Применить миграции
python manage.py migrate
```

### Управление статикой
```bash
# Собрать статические файлы
python manage.py collectstatic
```

## 🔧 Разработка и отладка

### Запуск с DEBUG режимом
```bash
DEBUG=True python manage.py runserver 0.0.0.0:8000
```

### Проверка конфигурации
```bash
python manage.py check
python manage.py check --deploy
```

### Django Shell
```bash
python manage.py shell

# Пример использования:
from clubs.models import Club
print(Club.objects.count())  # Должно показать 7
```

## 📊 Мониторинг в реальном времени

### Просмотр логов
```bash
# В отдельном терминале
tail -f unitysphere.log
```

### Проверка AI API
```bash
# Тестовый запрос к AI чату
curl -X POST http://localhost:8000/api/v1/ai/chat/ \
     -H "Content-Type: application/json" \
     -d '{"message": "Привет!"}'
```

## 🚨 Известные проблемы и решения

### 1. AI API ошибка
**Проблема:** AttributeError в AI чате
**Решение:** AI работает в fallback режиме - это нормально для разработки

### 2. Нет PostgreSQL
**Решение:** Для локальной разработки используется SQLite - это нормально

### 3. Нет Redis
**Решение:** Для локальной разработки кэш работает в памяти

## 🎯 Что можно тестировать:

### 1. Основной сайт
- Откройте http://localhost:8000
- Проверьте отображение клубов
- Протестируйте навигацию

### 2. API тесты
```bash
# Получить список клубов
curl http://localhost:8000/api/v1/clubs/

# Получить детальную информацию о клубе
curl http://localhost:8000/api/v1/clubs/c9fa4ac3-e77a-4f78-81fc-69212ecfe7af/
```

### 3. Админ панель
- Перейдите в http://localhost:8000/admin/
- Войдите с созданным суперпользователем
- Управляйте клубами и пользователями

## 📋 Проверочный лист перед работой:

- [x] Виртуальное окружение активировано
- [x] Зависимости установлены
- [x] Миграции применены
- [x] Статика собрана
- [x] Сервер запущен
- [x] Сайт доступен по localhost:8000

## 🚀 Production подготовка

Когда будете готовы к production:

1. **Установите PostgreSQL:**
```bash
sudo apt install postgresql postgresql-contrib
sudo -u postgres createdb unitysphere_prod
```

2. **Установите Redis:**
```bash
sudo apt install redis-server
sudo systemctl start redis
```

3. **Используйте production скрипт:**
```bash
sudo ./deploy.sh production
```

## 💡 Советы по разработке:

1. **Всегда работайте в виртуальном окружении**
2. **Сохраняйте миграции в git**
3. **Тестируйте API перед изменениями**
4. **Используйте DEBUG=True для отладки**
5. **Проверяйте логи при проблемах**

---
**🎉 Готово! Ваш локальный сайт UnitySphere готов к работе!**

Для начала разработки просто выполните:
```bash
source venv/bin/activate && python manage.py runserver
```