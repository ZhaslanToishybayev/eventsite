# 🏠 Локальная настройка и запуск UnitySphere

## 🚀 Быстрый старт

### 1. Активация виртуального окружения
```bash
source venv/bin/activate
```

### 2. Проверка конфигурации
```bash
python manage.py check
```

### 3. Запуск сервера разработки
```bash
python manage.py runserver
```

Теперь приложение доступно по адресу: http://localhost:8000

## 📊 Создание тестовых данных

### Создание суперпользователя
```bash
python manage.py createsuperuser
```

### Создание тестовых данных
```bash
python manage.py shell
```

```python
# Создание тестовых клубов
from clubs.models import Club, ClubCategory
from accounts.models import User

# Создаем категории
cat1, created = ClubCategory.objects.get_or_create(name='Технологии')
cat2, created = ClubCategory.objects.get_or_create(name='Бизнес')
cat3, created = ClubCategory.objects.get_or_create(name='Творчество')

# Создаем тестовый клуб (если нужно)
# Выполняйте только если у вас есть пользователь
```

## 🔧 Проверка AI консультанта

После запуска сервера:

1. Откройте http://localhost:8000
2. Найдите виджет AI консультанта
3. Протестируйте отправку сообщения

API эндпоинты для тестирования:
- `GET http://localhost:8000/api/v1/welcome-message/` - Приветствие
- `POST http://localhost:8000/api/v1/chat/` - Отправка сообщения в чат
- `GET http://localhost:8000/api/v1/platform-services/` - Список услуг

## 🐛 Поиск проблем

Если что-то не работает:

```bash
# Проверка миграций
python manage.py showmigrations

# Применение миграций
python manage.py migrate

# Проверка статических файлов
python manage.py collectstatic --noinput

# Создание таблиц для нового функционала
python manage.py makemigrations accounts clubs ai_consultant
python manage.py migrate
```

## 📝 Примечания

- Для локальной разработки используется SQLite (db.sqlite3)
- PostgreSQL и Redis нужны только для production
- AI консультант сейчас в fallback режиме
- Все модули безопасности уже работают

## 🎯 Следующие шаги

1. ✅ Настроить локальное окружение - ГОТОВО
2. ⏳ Протестировать функционал
3. ⏳ Настроить PostgreSQL (опционально для продакшена)
4. ⏳ Настроить Redis (опционально для продакшена)