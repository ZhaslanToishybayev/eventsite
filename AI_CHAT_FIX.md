# AI Chat Empty Response - Quick Fix

## Проблема
OpenAI возвращает пустой ответ с ошибкой: "model output must contain either output text or tool calls"

## Возможные причины

### 1. Модель gpt-3.5-turbo может иметь проблемы с инструментами
Текущая модель: `gpt-3.5-turbo`

### 2. Решения (выберите одно):

#### Вариант A: Обновить модель (Рекомендуется)
Измените в `.env`:
```bash
OPENAI_MODEL=gpt-4o-mini
```

Или:
```bash
OPENAI_MODEL=gpt-4-turbo
```

#### Вариант B: Временно отключить инструменты для club_specialist
Это позволит AI отвечать текстом без вызова инструментов.

Измените `ai_consultant/agents/specialists/club_agent.py`:
```python
def get_tools(self):
    """Return empty tools list temporarily"""
    return []  # Temporarily disable tools
```

#### Вариант C: Использовать fallback без инструментов
Добавить обработку ошибки в `chat.py` чтобы при пустом ответе повторить запрос без инструментов.

## Рекомендация
**Вариант A** - обновите модель на `gpt-4o-mini`. Она лучше работает с инструментами и дешевле чем gpt-4.

После изменения `.env` перезапустите сервер:
```bash
./venv/bin/python manage.py runserver
```

## Проверка логов
После отправки сообщения в чат, проверьте терминал с runserver на наличие логов:
- 📤 Sending to OpenAI
- 📝 User message
- 📥 OpenAI response
- ❌ Errors (если есть)
