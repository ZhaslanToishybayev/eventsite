"""
🎯 Чеклист контроля качества ИИ-консультанта платформы "ЦЕНТР СОБЫТИЙ"
Комплексная система оценки и улучшения качества ответов
"""

class AIQualityChecklist:
    """
    Детальный чеклист для оценки качества работы ИИ-консультанта
    """

    # =====================================================
    # ОСНОВНЫЕ МЕТРИКИ КАЧЕСТВА
    # =====================================================

    QUALITY_METRICS = {
        "platform_knowledge": {
            "weight": 30,
            "description": "Знание платформы и ее возможностей"
        },
        "communication_style": {
            "weight": 20,
            "description": "Соответствие стилю общения платформы"
        },
        "action_orientation": {
            "weight": 25,
            "description": "Ориентация на конкретные действия"
        },
        "personalization": {
            "weight": 15,
            "description": "Персонализация ответов"
        },
        "error_handling": {
            "weight": 10,
            "description": "Обработка ошибок и нестандартных ситуаций"
        }
    }

    # =====================================================
    # ПОЛНЫЙ ЧЕК-ЛИСТ КАЧЕСТВА
    # =====================================================

    COMPREHENSIVE_CHECKLIST = {
        "platform_knowledge": {
            "basic_info": [
                "✅ Знает название платформы 'ЦЕНТР СОБЫТИЙ'",
                "✅ Понимает, что это платформа Казахстана",
                "✅ Знает основную миссию: объединение единомышленников",
                "✅ Понимает главный слоган: 'ВЫБИРАЙ ОКРУЖЕНИЕ, КОТОРОЕ ПОМОЖЕТ ТЕБЕ РАСТИ'"
            ],
            "categories": [
                "✅ Знает 3 основные категории: Спорт, Хобби, Профессия",
                "✅ Может привести примеры клубов в каждой категории",
                "✅ Понимает преимущества каждой категории",
                "✅ Знает теги для поиска в каждой категории"
            ],
            "features": [
                "✅ Понимает концепцию сообществ (клубов)",
                "✅ Знает про поиск единомышленников",
                "✅ Понимает систему мероприятий",
                "✅ Знает про услуги клубов",
                "✅ Понимает систему партнерства"
            ],
            "instructions": [
                "✅ Может объяснить процесс создания клуба",
                "✅ Знает обязательные поля для создания клуба",
                "✅ Понимает разницу между публичными и приватными клубами",
                "✅ Может объяснить процесс вступления в клуб"
            ]
        },

        "communication_style": {
            "orchestrator": [
                "✅ Использует дружелюбный, вдохновляющий тон",
                "✅ Обращается на 'ты'",
                "✅ Использует эмодзи для живости",
                "✅ Подчеркивает ценность сообщества",
                "✅ Мотивирует к действию"
            ],
            "club_specialist": [
                "✅ Использует экспертный, проактивный стиль",
                "✅ Действует быстро и эффективно",
                "✅ Минимальное использование эмодзи",
                "✅ Фокусируется на результате",
                "✅ Не отвлекается на посторонние темы"
            ],
            "support_specialist": [
                "✅ Использует терпеливый и дружелюбный тон",
                "✅ Дает пошаговые инструкции",
                "✅ Приводит конкретные примеры",
                "✅ Проверяет понимание",
                "✅ Предлагает дополнительную помощь"
            ],
            "mentor_specialist": [
                "✅ Использует вдохновляющий тон",
                "✅ Использует эмодзи: 🎯, 📈, 🏆",
                "✅ Персонализирует рекомендации",
                "✅ Празднует достижения",
                "✅ Дает конкретные советы"
            ]
        },

        "action_orientation": [
            "✅ Дает конкретные, выполнимые шаги",
            "✅ Использует призывы к действию",
            "✅ Предлагает следующие шаги",
            "✅ Ссылается на реальные возможности платформы",
            "✅ Избегает абстрактных рекомендаций"
        ],

        "personalization": [
            "✅ Учитывает контекст предыдущих сообщений",
            "✅ Адаптирует ответы под уровень пользователя",
            "✅ Предлагает релевантные варианты",
            "✅ Запоминает предпочтения пользователя",
            "✅ Дает персонализированные рекомендации"
        ],

        "error_handling": [
            "✅ Вежливо признает отсутствие информации",
            "✅ Предлагает альтернативные решения",
            "✅ Направляет к нужному специалисту",
            "✅ Сохраняет позитивный тон",
            "✅ Предлагает связаться с поддержкой"
        ]
    }

    # =====================================================
    # ТЕСТОВЫЕ СЦЕНАРИИ ДЛЯ ПРОВЕРКИ
    # =====================================================

    TEST_SCENARIOS = {
        "basic_knowledge": [
            {
                "question": "Что такое ЦЕНТР СОБЫТИЙ?",
                "expected_elements": [
                    "Справочник организаций досуга и профессиональной деятельности Казахстана",
                    "Объединение единомышленников",
                    "3 категории: Спорт, Хобби, Профессия",
                    "Главный слоган"
                ]
            },
            {
                "question": "Какие виды клубов у вас есть?",
                "expected_elements": [
                    "Перечисление 3 категорий",
                    "Примеры в каждой категории",
                    "Преимущества каждой категории",
                    "Теги для поиска"
                ]
            },
            {
                "question": "Как создать клуб?",
                "expected_elements": [
                    "Пошаговая инструкция",
                    "Обязательные поля",
                    "Советы по созданию",
                    "Предложение помощи"
                ]
            }
        ],

        "agent_routing": [
            {
                "question": "Привет!",
                "expected_agent": "orchestrator",
                "response_type": "Приветствие и общий обзор"
            },
            {
                "question": "Создай клуб по шахматам",
                "expected_agent": "club_specialist",
                "response_type": "Создание клуба с минимальными вопросами"
            },
            {
                "question": "Не могу войти в аккаунт",
                "expected_agent": "support_specialist",
                "response_type": "Пошаговая инструкция по решению"
            },
            {
                "question": "Хочу развиваться в IT",
                "expected_agent": "mentor_specialist",
                "response_type": "Рекомендации по развитию"
            }
        ],

        "complex_scenarios": [
            {
                "question": "Я программист, хочу найти единомышленников и создать IT-клуб",
                "expected_flow": [
                    "Распознавание множественного запроса",
                    "Маршрутизация к club_specialist",
                    "Предложение поиска IT-клубов",
                    "Предложение создания нового клуба",
                    "Конкретные шаги по созданию"
                ]
            },
            {
                "question": "Какие клубы помогают развить лидерство?",
                "expected_flow": [
                    "Маршрутизация к mentor_specialist",
                    "Анализ навыка 'лидерство'",
                    "Рекомендации по категориям клубов",
                    "Конкретные примеры клубов",
                    "План развития"
                ]
            }
        ]
    }

    # =====================================================
    # КРИТЕРИИ ОЦЕНКИ
    # =====================================================

    SCORING_CRITERIA = {
        "excellent": {
            "score_range": (90, 100),
            "description": "Идеальный ответ - полный эксперт платформы"
        },
        "good": {
            "score_range": (75, 89),
            "description": "Хороший ответ - знает основы, но есть пробелы"
        },
        "satisfactory": {
            "score_range": (60, 74),
            "description": "Удовлетворительный ответ - базовые знания, нужны улучшения"
        },
        "needs_improvement": {
            "score_range": (40, 59),
            "description": "Требует улучшений - существенные пробелы в знаниях"
        },
        "poor": {
            "score_range": (0, 39),
            "description": "Плохой ответ - не знает платформу"
        }
    }

    @classmethod
    def evaluate_response(cls, question: str, response: str, expected_agent: str = None) -> dict:
        """
        Оценить ответ ИИ по комплексным критериям

        Args:
            question: Вопрос пользователя
            response: Ответ ИИ
            expected_agent: Ожидаемый тип агента

        Returns:
            dict: Результат оценки с баллами и рекомендациями
        """

        score = 0
        max_score = 100
        feedback = []

        # Проверка знаний платформы
        platform_score = cls._check_platform_knowledge(response)
        score += platform_score * 0.30

        # Проверка стиля общения
        style_score = cls._check_communication_style(response, expected_agent)
        score += style_score * 0.20

        # Проверка ориентации на действие
        action_score = cls._check_action_orientation(response)
        score += action_score * 0.25

        # Проверка персонализации
        personalization_score = cls._check_personalization(question, response)
        score += personalization_score * 0.15

        # Проверка обработки ошибок
        error_score = cls._check_error_handling(response)
        score += error_score * 0.10

        # Определение уровня качества
        quality_level = cls._get_quality_level(score)

        # Генерация рекомендаций
        recommendations = cls._generate_recommendations(score, feedback)

        return {
            "total_score": round(score, 1),
            "quality_level": quality_level,
            "scores": {
                "platform_knowledge": round(platform_score, 1),
                "communication_style": round(style_score, 1),
                "action_orientation": round(action_score, 1),
                "personalization": round(personalization_score, 1),
                "error_handling": round(error_score, 1)
            },
            "feedback": feedback,
            "recommendations": recommendations
        }

    @classmethod
    def _check_platform_knowledge(cls, response: str) -> float:
        """Проверить знание платформы"""
        score = 0
        max_score = 100

        # Базовая информация
        if "ЦЕНТР СОБЫТИЙ" in response:
            score += 15
        if "Казахстан" in response:
            score += 5
        if "ВЫБИРАЙ ОКРУЖЕНИЕ" in response:
            score += 10

        # Категории
        categories = ["Спорт", "Хобби", "Профессия"]
        found_categories = sum(1 for cat in categories if cat in response)
        score += found_categories * 10  # до 30 баллов

        # Функционал
        if any(keyword in response for keyword in ["клуб", "сообщество"]):
            score += 10
        if "единомышленник" in response:
            score += 10
        if "мероприяти" in response:
            score += 5
        if "создани" in response and "клуб" in response:
            score += 15

        return min(score, max_score)

    @classmethod
    def _check_communication_style(cls, response: str, agent_type: str) -> float:
        """Проверить стиль общения"""
        score = 0
        max_score = 100

        # Общие критерии
        if "ты" in response:
            score += 20
        if any(emoji in response for emoji in ["😊", "🎯", "🏆", "📈", "🚀", "🌟"]):
            score += 10

        # Специфичные для агентов критерии
        if agent_type == "orchestrator":
            if any(word in response.lower() for word in ["добро пожаловать", "вдохновля", "мотивиру"]):
                score += 30
        elif agent_type == "club_specialist":
            if "создам" in response or "найду" in response:
                score += 30
        elif agent_type == "support_specialist":
            if "шаг" in response or "инструкция" in response:
                score += 30
        elif agent_type == "mentor_specialist":
            if any(word in response.lower() for word in ["развив", "расти", "навык"]):
                score += 30

        # Позитивный тон
        if any(word in response for word in ["отлично", "хорошо", "прекрасно", "замечательно"]):
            score += 10

        return min(score, max_score)

    @classmethod
    def _check_action_orientation(cls, response: str) -> float:
        """Проверить ориентацию на действие"""
        score = 0
        max_score = 100

        # Призывы к действию
        action_phrases = [
            "давай", "начни", "создай", "найди", "вступи",
            "попробуй", "регистрируйся", "перейди", "нажми"
        ]
        score += sum(10 for phrase in action_phrases if phrase in response.lower())

        # Конкретные шаги
        if "шаг" in response.lower():
            score += 20
        if any(digit in response for digit in ["1", "2", "3", "4", "5"]):
            score += 15

        # Ссылки на функционал
        if "раздел" in response.lower():
            score += 15

        return min(score, max_score)

    @classmethod
    def _check_personalization(cls, question: str, response: str) -> float:
        """Проверить персонализацию"""
        score = 0
        max_score = 100

        # Учет контекста вопроса
        question_words = set(question.lower().split())
        response_words = set(response.lower().split())
        overlap = len(question_words & response_words)
        score += min(overlap * 5, 30)

        # Избегание общих фраз
        generic_phrases = ["интересные клубы", "хорошие клубы", "наши клубы"]
        generic_count = sum(1 for phrase in generic_phrases if phrase in response.lower())
        score -= generic_count * 10

        # Конкретизация
        if "конкретн" in response.lower():
            score += 20
        if any(cat in response for cat in ["Спорт", "Хобби", "Профессия"]):
            score += 20

        # Предложения вариантов
        if "например" in response.lower():
            score += 10

        return max(min(score, max_score), 0)

    @classmethod
    def _check_error_handling(cls, response: str) -> float:
        """Проверить обработку ошибок"""
        score = 100  # Начальный балл

        # Снижение баллов за проблемы
        if "не знаю" in response.lower():
            score -= 50
        if "не могу" in response.lower():
            score -= 30
        if "попробуйте" in response.lower() and "позже" in response.lower():
            score -= 20

        # Повышение баллов за хорошее решение
        if "связаться с поддержкой" in response.lower():
            score += 20
        if "альтернатив" in response.lower():
            score += 15

        return max(score, 0)

    @classmethod
    def _get_quality_level(cls, score: float) -> str:
        """Определить уровень качества"""
        for level, criteria in cls.SCORING_CRITERIA.items():
            if criteria["score_range"][0] <= score <= criteria["score_range"][1]:
                return level
        return "poor"

    @classmethod
    def _generate_recommendations(cls, score: float, feedback: list) -> list:
        """Сгенерировать рекомендации по улучшению"""
        recommendations = []

        if score < 90:
            if score < 60:
                recommendations.append("🚨 Критично: Изучить полную базу знаний платформы")

            if score < 75:
                recommendations.append("📚 Улучшить знание функций платформы")

            if score < 85:
                recommendations.append("🎯 Добавить больше конкретных действий")

            if score < 90:
                recommendations.append("💬 Персонализировать ответы под контекст")
        else:
            recommendations.append("🌟 Отличный ответ! Продолжайте в том же духе")

        return recommendations

    @classmethod
    def run_comprehensive_test(cls) -> dict:
        """
        Запустить комплексное тестирование

        Returns:
            dict: Результаты тестирования всех сценариев
        """
        results = {
            "total_tests": 0,
            "passed_tests": 0,
            "failed_tests": 0,
            "average_score": 0,
            "test_results": [],
            "summary": {}
        }

        total_score = 0

        for category, scenarios in cls.TEST_SCENARIOS.items():
            category_results = []

            for scenario in scenarios:
                # Здесь должна быть реальная проверка ответа ИИ
                # Для демонстрации используем моковые данные
                mock_score = 85  # Placeholder
                mock_response = "Моковый ответ для тестирования"

                test_result = {
                    "category": category,
                    "question": scenario["question"],
                    "score": mock_score,
                    "quality_level": cls._get_quality_level(mock_score),
                    "response": mock_response
                }

                category_results.append(test_result)
                total_score += mock_score
                results["total_tests"] += 1

                if mock_score >= 75:
                    results["passed_tests"] += 1
                else:
                    results["failed_tests"] += 1

            results["test_results"].extend(category_results)

        if results["total_tests"] > 0:
            results["average_score"] = total_score / results["total_tests"]

        # Генерация сводки
        results["summary"] = {
            "overall_quality": cls._get_quality_level(results["average_score"]),
            "improvement_areas": cls._identify_improvement_areas(results),
            "next_steps": cls._get_next_steps(results["average_score"])
        }

        return results

    @classmethod
    def _identify_improvement_areas(cls, results: dict) -> list:
        """Определить области для улучшения"""
        areas = []

        if results["average_score"] < 80:
            areas.append("База знаний платформы")
        if results["average_score"] < 85:
            areas.append("Стиль общения")
        if results["average_score"] < 90:
            areas.append("Персонализация ответов")

        return areas

    @classmethod
    def _get_next_steps(cls, average_score: float) -> list:
        """Получить следующие шаги"""
        if average_score >= 90:
            return [
                "✅ Поддерживать высокий стандарт",
                "🔄 Регулярно обновлять знания",
                "📊 Отслеживать качество ответов"
            ]
        elif average_score >= 75:
            return [
                "📚 Доработать отдельные аспекты",
                "🎯 Сфокусироваться на слабых местах",
                "✨ Добавить больше персонализации"
            ]
        else:
            return [
                "🚨 Срочно улучшить базу знаний",
                "📖 Пройти полное обучение по платформе",
                "🔄 Пересмотреть все промпты"
            ]


# Глобальный экземпляр для удобного использования
quality_checker = AIQualityChecklist()