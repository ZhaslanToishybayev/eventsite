# 🛠️ MCP Инструменты для Claude Code

## ✅ **Установленные бесплатные AI инструменты**

### 🤖 **MCP Серверы** (Model Context Protocol)

#### 1. **Serena AI** - Анализатор кода ✅
- **Порт:** 8001
- **Функции:** Анализ Django кода, документация, поиск символов
- **Использование:** Уже интегрирована в проект
- **API:** `GET /api/v1/ai/serena/status/`

#### 2. **Filesystem MCP Server** ✅
- **Функции:** Работа с файловой системой проекта
- **Использование:** `npx @modelcontextprotocol/server-filesystem /path/to/project`
- **Примение:** Поиск файлов, чтение содержимого, навигация

#### 3. **Code Runner MCP Server** ✅
- **Функции:** Выполнение кода в изолированной среде
- **Использование:** `npx mcp-server-code-runner`
- **Примение:** Тестирование фрагментов кода, скриптов

#### 4. **Chrome DevTools MCP Server** ✅
- **Функции:** Отладка и автоматизация браузера
- **Использование:** `npx chrome-devtools-mcp`
- **Примение:** Тестирование UI, скриншоты, веб-автоматизация

#### 5. **Web Research MCP Server** ✅
- **Функции:** Веб-исследования и парсинг
- **Использование:** `npx @mzxrai/mcp-webresearch`
- **Примение:** Поиск информации, анализ веб-страниц

### 🛠️ **Локальные инструменты**

#### 6. **Aider** ✅
- **Функции:** Автоматический рефакторинг кода
- **Установка:** `pip install aider` (в venv)
- **Использование:** `source venv/bin/activate && aider`
- **Примение:** Рефакторинг, улучшение кода, интеграция с Git

## 🚀 **Быстрый старт**

### Запуск всех MCP серверов:
```bash
./start_mcp_servers.sh
```

### Остановка всех MCP серверов:
```bash
./stop_mcp_servers.sh
```

### Проверка статуса:
```bash
# Проверка Serena AI
curl http://localhost:8001/health

# Проверка процессов
ps aux | grep mcp
```

## 📋 **Конфигурация Claude Desktop**

Файл конфигурации: `claude_desktop_config.json`

```json
{
  "mcpServers": {
    "serena": {...},
    "filesystem": {...},
    "code-runner": {...},
    "chrome-devtools": {...},
    "web-research": {...}
  }
}
```

## 💡 **Как использовать**

### В Claude Code я могу:

1. **Анализировать код** через Serena AI
2. **Искать файлы** через Filesystem MCP
3. **Выполнять код** через Code Runner
4. **Тестировать UI** через Chrome DevTools
5. **Исследовать веб** через Web Research
6. **Рефакторить** через Aider

### Примеры запросов:

- "Проанализируй модель User в Django"
- "Найди все файлы с тестами"
- "Выполни этот Python скрипт"
- "Сделай скриншот главной страницы"
- "Найди информацию о Django REST Framework"
- "Рефакторь функцию просмотра клубов"

## 🔧 **Требования**

- Node.js 22.20.0 ✅
- Python 3.11+ ✅
- uv пакетный менеджер ✅
- Интернет соединение (для некоторых MCP серверов)

## 📊 **Статус интеграции**

- ✅ Serena AI - полностью интегрирована
- ✅ Aider - установлен и готов к использованию
- ✅ MCP серверы - настроены и готовы
- ⚠️ Ollama - требует права администратора

## 🎯 **Практическое применение для UnitySphere**

1. **Анализ Django моделей** - Serena AI
2. **Рефакторинг представлений** - Aider
3. **Тестирование API** - Code Runner
4. **Отладка фронтенда** - Chrome DevTools
5. **Документация** - Web Research + Serena AI

**Все инструменты готовы к использованию! 🚀**